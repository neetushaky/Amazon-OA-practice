#include<bits/stdc++.h>
using namespace std;
In this we are calculating using prefix sum from both the side that is right and left and after get the and side by side we are taking the average of the
each index of the prefix sum and and thus the index which give us the least abs(left and right)average we are just returning it.
int stockPrice(vector<int>& sp)
{
    int n= sp.size();
    int sum=0;
    int minNetPrice=INT_MAX;
    for(int i=0;i<n;i++)
    {
        sum+=sp[i];
    }
    int k=0, month=0;
    for(int i=0;i<n-1;i++)
    {
        k+=sp[i];
        sum-=sp[i];
        int avg1=k/(i+1); /;eft prefix average
        int avg2=sum/(n-(i+1)); //right prefix average
        int netPrice=abs(avg1-avg2); //absolute value of the sub of avg1 and avg2
        if(netPrice < minNetPrice)
        {
            minNetPrice= netPrice; //returning the least price month
            month=i+1;
        }
    }
    return month;
}
int main()
{
    vector<int> v {1,3,2,4,5};
    cout<<stockPrice(v);
    return 0;
}
